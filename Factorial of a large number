class Solution:
    def factorial(self, N):
        sum=1
        s=[]
        for i in range(1,N+1):
            sum=sum*i
            
        s.append(sum)
        return s
        #code here
        
Time complexity: O(n)
